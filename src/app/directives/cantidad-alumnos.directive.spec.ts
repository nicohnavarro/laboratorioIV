import { CantidadAlumnosDirective } from './cantidad-alumnos.directive';

describe('CantidadAlumnosDirective', () => {
  it('should create an instance', () => {
    const directive = new CantidadAlumnosDirective();
    expect(directive).toBeTruthy();
  });
});
